import axios from "axios";

export default function useWeather(){
  const fetchWeather = async () => {
    try {
        
    } catch (error) {
      console.log(error);
    }
  }

  return{
    fetchWeather
  }
}